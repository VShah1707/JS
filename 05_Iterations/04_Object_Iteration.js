const myObject  = {
    js : 'Javascript',
    cpp : "C++",
    rb : 'Ruby',
    swift : 'Swift'
}

const myArr = [1,2,3,4,5,6,]

for (const key in myArr) {
    // console.log(key," :-",myArr[key]);
}

// behind the scenes array stores the values as
const log = console.log

