// for of
const numbers = [1,2,3,4,5,5]
for (const num of numbers) {
    // console.log(num);
}


// Maps : It holds the key value pair & remebers the original insertion order of the keys. Also it hold only unique values
// also Map is not iterable
const map = new Map()
map.set('fname', 'varshil')
map.set('fname', 'Keta')
// map.set('fname', 'varshil') this will not be added in the map


