const vowels = ['a','e','i','o','u']
const numbers = [1,5,6,3,9]

// concat : return new array
// spread Operator (...) 
// flat : used to flatten all the nested arrays and returns a new array
// Array.its-methods ==> check it 

// console.log(vowels.concat(numbers));

